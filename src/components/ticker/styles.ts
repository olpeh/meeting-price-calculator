import { stylesheet } from 'typestyle';

export const styles = stylesheet({
  actualPrice: {
    justifyContent: 'center'
  },
  priceLabel: {
    fontSize: '3vw',
    textAlign: 'center'
  },
  priceValue: {
    fontWeight: 'bold',
    fontSize: '10vw'
  }
});
